#include "main.h"
#include <unistd.h>

/**
 * mul - check description
 * Description: multiplies two integers
 * Return: Always 0
 * @a: input
 * @b: input
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
